# templates/certbot_nginx.conf.j2

# HTTP server block for certbot
server {
    listen 80;
    server_name {{ domain_name }};

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 404;  # Only allow certbot validation
    }
}
